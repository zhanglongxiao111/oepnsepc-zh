name: tdd
version: 1
description: 测试驱动开发工作流 - tests → implementation → docs
artifacts:
  - id: spec
    generates: spec.md
    description: 定义需求的功能规范
    template: spec.md
    instruction: |
      创建定义要构建什么的功能规范。

      章节：
      - **Feature**: 功能的名称和高层描述，说明其目的和用户价值
      - **Requirements**: 具体需求列表。使用 SHALL/MUST 进行规范性语言。
      - **Acceptance Criteria**: 使用 WHEN/THEN 格式的可测试标准

      格式要求：
      - 每个需求应该是具体且可测试的
      - 使用 `#### Scenario: <名称>` 和 WHEN/THEN 格式编写验收标准
      - 明确定义边缘情况和错误场景
      - 每个需求必须至少有一个场景

      示例：
      ```
      ## Feature: 用户认证

      用户可以安全地登录应用程序。

      ## Requirements

      ### Requirement: 密码验证
      系统 SHALL 验证密码满足最低安全要求。

      #### Scenario: 有效密码被接受
      - **WHEN** 密码有 8+ 个字符、大写、小写和数字
      - **THEN** 密码被接受

      #### Scenario: 弱密码被拒绝
      - **WHEN** 密码少于 8 个字符
      - **THEN** 系统显示"密码太短"错误
      ```

      此规范驱动测试创建 - 每个场景成为一个测试用例。
    requires: []

  - id: tests
    generates: "tests/*.test.ts"
    description: 在实现之前编写的测试文件
    template: test.md
    instruction: |
      在实现之前编写测试（TDD 红色阶段）。

      文件命名：
      - 创建测试文件为 `tests/<feature>.test.ts`
      - 每个功能/能力一个测试文件
      - 使用与规范匹配的描述性名称

      测试结构：
      - 使用与规范场景匹配的 Given/When/Then 格式
      - 使用 `describe()` 块分组相关测试
      - 规范中的每个场景至少成为一个 `it()` 测试

      覆盖要求：
      - 覆盖规范中的每个需求
      - 包含正常路径（成功案例）
      - 包含边缘情况（边界条件）
      - 包含错误场景（无效输入、失败）
      - 测试最初应该失败（还没有实现）

      示例：
      ```typescript
      describe('密码验证', () => {
        it('接受满足所有要求的有效密码', () => {
          // GIVEN 一个满足所有要求的密码
          const password = 'SecurePass1';
          // WHEN 验证
          const result = validatePassword(password);
          // THEN 应该被接受
          expect(result.valid).toBe(true);
        });

        it('拒绝少于 8 个字符的密码', () => {
          // GIVEN 一个短密码
          const password = 'Short1';
          // WHEN 验证
          const result = validatePassword(password);
          // THEN 应该被拒绝并显示消息
          expect(result.valid).toBe(false);
          expect(result.error).toBe('密码太短');
        });
      });
      ```

      精确遵循规范需求 - 测试验证规范。
    requires:
      - spec

  - id: implementation
    generates: "src/*.ts"
    description: 通过测试的实现代码
    template: implementation.md
    instruction: |
      实现功能以使测试通过（TDD 绿色阶段）。

      TDD 工作流：
      1. 运行测试 - 确认它们失败（红色）
      2. 编写最少的代码通过一个测试
      3. 运行测试 - 确认该测试通过（绿色）
      4. 如需要则重构，同时保持测试绿色
      5. 对下一个失败的测试重复

      实现指南：
      - 编写最少的代码通过每个测试 - 不多不少
      - 频繁运行测试以验证进度
      - 保持函数小巧专注
      - 使用清晰、描述性的名称

      代码组织：
      - 在 `src/<feature>.ts` 中创建源文件
      - 清晰地导出公共 API
      - 保持实现细节私有
      - 为公共函数添加 JSDoc 注释

      示例结构：
      ```typescript
      /**
       * 验证密码满足安全要求。
       * @param password - 要验证的密码
       * @returns 包含 valid 标志和可选 error 的验证结果
       */
      export function validatePassword(password: string): ValidationResult {
        if (password.length < 8) {
          return { valid: false, error: '密码太短' };
        }
        // ... 额外检查
        return { valid: true };
      }
      ```

      不要过度工程化 - 只实现测试要求的内容。
    requires:
      - tests

  - id: docs
    generates: "docs/*.md"
    description: 已实现功能的文档
    template: docs.md
    instruction: |
      为已实现的功能编写文档。

      章节：
      - **Overview**: 功能做什么以及为什么存在（1-2 段）
      - **Getting Started**: 立即使用功能的快速入门指南
      - **Examples**: 展示常见用例的代码示例
      - **Reference**: 详细的 API 文档、配置选项

      指南：
      - 为用户而不是开发者编写
      - 从最常见的用例开始
      - 包含可复制粘贴的代码示例
      - 记录所有配置选项及默认值
      - 注明任何限制、边缘情况或注意事项
      - 链接到相关功能或规范

      示例结构：
      ```markdown
      ## Overview

      密码验证确保用户密码在创建账户或更改密码之前
      满足安全要求。

      ## Getting Started

      导入并使用验证函数：

      ```typescript
      import { validatePassword } from './password';

      const result = validatePassword('MySecurePass1');
      if (!result.valid) {
        console.error(result.error);
      }
      ```

      ## Examples

      ### 基本验证
      ...

      ### 自定义错误处理
      ...

      ## Reference

      ### validatePassword(password)

      | 参数 | 类型 | 描述 |
      |------|------|------|
      | password | string | 要验证的密码 |

      **返回**: `{ valid: boolean, error?: string }`
      ```

      引用规范了解需求，引用实现了解细节。
    requires:
      - implementation
